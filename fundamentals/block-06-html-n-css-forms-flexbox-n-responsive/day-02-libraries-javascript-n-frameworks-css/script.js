window.onload = function() {

    const body = document.body;

    let sendButton = document.querySelector('.buttons');

    sendButton.addEventListener('click', function(event) {
        event.preventDefault();
    })
}